# duplicate for use. edit the values before running the pipeline

# path one: reproduce from your own dump (requires a folder containing raw/ and processed/)
DATA_DUMP=

# path two: use the existing dataset from hugging face
# optional override of author dataset (defaults to outofaditya/overflow-labs-dump)
HF_TOKEN=
HF_REPO_ID=outofaditya/overflow-labs-dump